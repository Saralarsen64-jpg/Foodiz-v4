import { useState } from "react";import { useNavigate } from "react-router-dom";import { ChevronLeft, Send } from "lucide-react";import { createSupportTicket } from "../../utils/supportStore";export default function CourierSupport(){const navigate=useNavigate();const [message,setMessage]=useState('');const [sent,setSent]=useState(false);const submit=()=>{createSupportTicket({role:'courier',subject:'Support livreur',message,userName:'Karim',userPhone:'+33600000000',city:'Paris',priority:'medium'});setSent(true)};return <div className="min-h-screen bg-foodiz-black pb-24"><header className="bg-foodiz-card border-b border-foodiz-gold/10 px-4 py-3 sticky top-0 z-30"><div className="max-w-lg mx-auto flex items-center gap-3"><button onClick={()=>navigate('/courier')} className="text-foodiz-gold"><ChevronLeft size={20}/></button><h1 className="foodiz-title text-lg">Support livreur</h1></div></header><main className="max-w-lg mx-auto px-4 py-6 space-y-4"><div className="foodiz-card p-4"><textarea value={message} onChange={(e)=>setMessage(e.target.value)} className="w-full min-h-[120px] bg-transparent text-foodiz-cream outline-none resize-none" placeholder="Décrivez votre problème..."/></div><button onClick={submit} className="w-full foodiz-btn py-4">{sent?'Ticket envoyé':'Envoyer au support'}</button></main></div>}