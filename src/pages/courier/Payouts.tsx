import { useState } from "react";import { useNavigate } from "react-router-dom";import { ChevronLeft, Clock, Calendar } from "lucide-react";export default function CourierPayouts(){const navigate=useNavigate();const [freq,setFreq]=useState<'daily'|'weekly'>('weekly');return <div className="min-h-screen bg-foodiz-black pb-24"><header className="bg-foodiz-card border-b border-foodiz-gold/10 px-4 py-3 sticky top-0 z-30"><div className="max-w-lg mx-auto flex items-center gap-3"><button onClick={()=>navigate('/courier')} className="text-foodiz-gold"><ChevronLeft size={20}/></button><h1 className="foodiz-title text-lg">Paiements</h1></div></header><main className="max-w-lg mx-auto px-4 py-6 space-y-4"><div className="foodiz-card p-6"><p className="text-3xl font-serif text-foodiz-gold mb-2">124,50 €</p><p className="text-xs text-foodiz-gray">Solde disponible</p></div><div className="grid grid-cols-2 gap-3"><button onClick={()=>setFreq('daily')} className={`foodiz-card p-4 ${freq==='daily'?'border-foodiz-gold/40 bg-foodiz-gold/5':''}`}>Quotidien</button><button onClick={()=>setFreq('weekly')} className={`foodiz-card p-4 ${freq==='weekly'?'border-foodiz-gold/40 bg-foodiz-gold/5':''}`}>Hebdomadaire</button></div></main></div>}