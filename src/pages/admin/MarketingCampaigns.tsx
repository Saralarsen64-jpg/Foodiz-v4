import { useMemo } from "react";import { useNavigate } from "react-router-dom";import { ChevronLeft, Megaphone } from "lucide-react";import { loadCampaigns } from "../../utils/marketingStore";export default function AdminMarketingCampaigns(){const navigate=useNavigate();const campaigns=useMemo(()=>loadCampaigns(),[]);return <div className="min-h-screen bg-foodiz-black text-foodiz-cream"><header className="bg-foodiz-card border-b border-foodiz-gold/10 px-6 py-4"><div className="max-w-6xl mx-auto flex items-center gap-3"><button onClick={()=>navigate('/admin')} className="text-foodiz-gold"><ChevronLeft size={20}/></button><h1 className="foodiz-title text-lg">Campagnes Foodiz+</h1></div></header><main className="max-w-6xl mx-auto p-6 space-y-3">{campaigns.length===0&&<div className="foodiz-card p-5 text-sm text-foodiz-gray">Aucune campagne.</div>}{campaigns.map((c)=><div key={c.id} className="foodiz-card p-4"><div className="flex items-center justify-between gap-4"><div><p className="text-sm font-medium">{c.establishmentName}</p><p className="text-[10px] text-foodiz-gray mt-1">{c.title}</p><p className="text-xs text-foodiz-gray mt-2">{c.message}</p></div><div className="text-right"><p className="text-[10px] uppercase text-foodiz-gold">{c.status}</p><p className="text-[10px] text-foodiz-gray mt-1">{c.sentAt}</p></div></div></div>)}</main></div>}